<!--
  Thanks for the PR!
  Fill out the sections below. Delete anything that doesn't apply.
-->

## Summary

<!-- One or two sentences: what does this PR do and why? -->


## Type of change

- [ ] Bug fix (non-breaking change that fixes an issue)
- [ ] New feature (non-breaking change that adds functionality)
- [ ] Breaking change (fix or feature that would cause existing behavior to change)
- [ ] Refactor (no behavioral change)
- [ ] Docs / chore / CI

## How was this tested?

<!-- Unit tests, manual verification steps, screenshots if UI. -->


## Checklist

- [ ] `npm run typecheck` passes locally
- [ ] `npm run lint` passes locally (zero errors)
- [ ] `npm test` passes locally
- [ ] `npm run build` succeeds locally
- [ ] No new `any` types introduced (or justified in the description)
- [ ] Any new env vars are added to `lib/env.ts` AND `.env.example`
- [ ] Any new PII fields are added to `PRIVACY.md` data map
- [ ] Migration files added to `lib/db/` if the schema changed

## Deployment / rollout notes

<!--
  Any new env vars to set in Vercel?
  Any migrations to run before deploy?
  Any feature flag / kill switch?
-->
